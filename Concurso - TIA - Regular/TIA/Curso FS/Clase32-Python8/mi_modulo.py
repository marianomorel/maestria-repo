# Estas funciones forman parte de un módulo.
# Se puede acceder a ellas desde otro script
# utilizando import y la notación punto.

def sumar(x, z):
    suma = x + z
    return suma

def restar(x, z):
    resta = x - z
    return resta
